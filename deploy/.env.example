# Copy to deploy/.env and adjust before starting the stack.
#
# Domain for the reverse proxy.
#   LAN/VPN pilot: any name that resolves to this host (or use the IP),
#   e.g. radar.localhost — TLS is self-signed (`tls internal` in Caddyfile).
#   Cloud/path B: a real domain you control, e.g. radar.example.com
CADDY_DOMAIN=radar.localhost

# Only used for Let's Encrypt when CADDY_DOMAIN is a real public domain
# (cloud path B). Leave empty for the LAN pilot.
CADDY_EMAIL=

# Basic-auth login for testers. The hash is a Caddy bcrypt hash:
#   deploy/hash-password.sh 'your-password'
RADAR_AUTH_USER=radiologist
RADAR_AUTH_HASH=